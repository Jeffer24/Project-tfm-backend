{
  "name": "web-proyecto-serverless",
  "version": "1.0.0",
  "main": "handler.js",
  "author": "Jefferson Rangel",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:dev": "serverless deploy --stage dev --region us-east-1",
    "deploy:stg": "serverless deploy --stage stg --region us-east-1",
    "offline": "serverless offline --stage dev --region us-east-1 --host 0.0.0.0 --lambdaPort 3301 --httpPort 3300"
  },
  "devDependencies": {
    "serverless-offline": "^9.1.6"
  },
  "dependencies": {
    "http-status-codes": "^2.2.0",
    "knex": "^2.2.0",
    "mysql": "^2.18.1"
  }
}
